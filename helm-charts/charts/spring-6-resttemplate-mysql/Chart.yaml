apiVersion: v2
name: spring-6-resttemplate-mysql
description: A Helm subchart for Mysql used in Spring 6 Reactive MongoDB application
type: application
# Disable Sonar warning for invalid semver: This value is set by Maven filtering
# and will be replaced with a valid version number during the build process.
version: @helm.chart.version@
appVersion: "8.4.5"
